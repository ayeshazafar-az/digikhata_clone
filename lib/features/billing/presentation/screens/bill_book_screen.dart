import 'package:flutter/material.dart';

class BillBookScreen extends StatelessWidget {
  const BillBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bill Book')),
      body: const Center(child: Text('Bill Book Module Coming Soon')),
    );
  }
}
