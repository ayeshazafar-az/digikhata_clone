import 'package:flutter/material.dart';

class StockBookScreen extends StatelessWidget {
  const StockBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Stock Book')),
      body: const Center(child: Text('Stock Book Module Coming Soon')),
    );
  }
}
